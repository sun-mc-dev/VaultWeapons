package me.sunmc.vw.model;

public enum AbilityType {
    /**
     * Per kill: crossbow reload time decreases, then auto-reloads.
     */
    CROSSBOW_RELOAD,
    /**
     * Per kill: sword Sharpness level increases by 1.
     */
    SWORD_SHARPNESS,
    NONE
}